package com.example.petquest.ui.theme

import androidx.compose.ui.graphics.Color

// ─── PetQuest Brand Palette ────────────────────────────────────────────────

// Primary — warm amber/orange (energetic, playful)
val PetOrange10   = Color(0xFF3D1A00)
val PetOrange20   = Color(0xFF6B2D00)
val PetOrange40   = Color(0xFFBF5C00)
val PetOrange80   = Color(0xFFFFB77A)
val PetOrange90   = Color(0xFFFFCFA8)   // slightly richer than before
val PetOrange95   = Color(0xFFFFEEDD)

// Secondary — nature green (calm, caring)
val PetGreen10    = Color(0xFF002112)
val PetGreen20    = Color(0xFF003822)
val PetGreen40    = Color(0xFF1A6B44)
val PetGreen80    = Color(0xFF70C99A)
val PetGreen90    = Color(0xFFACE6C5)
val PetGreen95    = Color(0xFFD2F4E3)

// Tertiary — playful pink (cute, pet-friendly)
val PetPink10     = Color(0xFF3E001E)
val PetPink20     = Color(0xFF650035)
val PetPink40     = Color(0xFFB0004D)
val PetPink80     = Color(0xFFFFB1C7)
val PetPink90     = Color(0xFFFFD9E3)
val PetPink95     = Color(0xFFFFECF0)

// Neutrals
val PetNeutral10  = Color(0xFF1C1B1E)
val PetNeutral20  = Color(0xFF313033)
val PetNeutral90  = Color(0xFFE6E1E6)
val PetNeutral95  = Color(0xFFF4EFF4)
val PetNeutral99  = Color(0xFFFFFBFF)

// Error
val PetError40    = Color(0xFFBA1A1A)
val PetError80    = Color(0xFFFFB4AB)
val PetError90    = Color(0xFFFFDAD6)

// ── Accent colors used by individual UI elements ───────────────────────────
// Stat cards (each stat gets its own identity)
val StatStreakOrange  = Color(0xFFE65100)   // deep orange — heat, energy
val StatBondGreen     = Color(0xFF2E7D32)   // forest green — growth, care
val StatLevelViolet   = Color(0xFF6A1B9A)   // purple — prestige, power

// Gold highlight (bond level 20+ border, special badge)
val GoldAccent        = Color(0xFFFFB300)
